const DAY = 3;
const input = Deno.readTextFileSync(`./day${DAY}/input`).trim().split("\n");

function solve(input: string[]) {
  const result = null;

  input.forEach((line) => {
    // draw owl
  });

  return result;
}

const test = solve([]);
console.log(`Test: ${test}`);

const answer = solve(input);
console.log(`Answer: ${answer}`);
